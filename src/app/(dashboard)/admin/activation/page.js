import ActivationPageWrapper from '@/components/dashboard/components/settings/activation';

export const metadata = {
    title: 'Activation',
};

export default function ActivationPage() {
    return <ActivationPageWrapper />;
}
