import { SocialLoginPageWrapper } from '@/components/dashboard/components/settings/general';

export const metadata = {
  title: 'Social Login',
  description: 'Configure social login settings',
};

export default function SocialLoginPage() {
  return <SocialLoginPageWrapper />;
}
