import { LanguagesPageWrapper } from '@/components/dashboard/components/settings/general';

export const metadata = {
    title: 'Languages',
};

export default function LanguagesPage() {
    return <LanguagesPageWrapper />;
}
