import { EarningsPageWrapper } from '@/components/dashboard/components';
import React from 'react';

export const metadata = { title: 'Earnings' };

function EarningsPage() {
    return (
        <EarningsPageWrapper />
    );
}

export default EarningsPage;
