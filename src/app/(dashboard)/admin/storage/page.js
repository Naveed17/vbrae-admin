import { StoragePageWrapper } from '@/components/dashboard/components/management-tools';

export const metadata = {
    title: 'Storage',
};

export default function StoragePage() {
    return <StoragePageWrapper />;
}
