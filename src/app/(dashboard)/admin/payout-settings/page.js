import { PayoutSettingsPageWrapper } from '@/components/dashboard/components';
export const metadata = { title: 'Payout Requests Settings' };
export default function PayoutSettingsPage() {
    return <PayoutSettingsPageWrapper />;
}
