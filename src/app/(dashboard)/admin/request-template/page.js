import { RequestTemplatePageWrapper } from '@/components/dashboard/components';
export const metadata = {
    title: 'Request Template',
    description: 'Request Template Page'
};

export default function RequestTemplatePage() {
    return <RequestTemplatePageWrapper />;
}
