import { PayoutRequestsPageWrapper } from '@/components/dashboard/components';
import React from 'react';

export const metadata = { title: 'Payout Requests' };

function PayoutRequestsPage() {
    return (
        <PayoutRequestsPageWrapper />
    );
}

export default PayoutRequestsPage;
