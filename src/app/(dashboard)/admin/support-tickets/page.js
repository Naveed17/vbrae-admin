import { SupportTicketsPageWrapper } from '@/components/dashboard/components';
export const metadata = {
    title: 'Support Tickets',
};

export default function SupportTicketsPage() {
    return <SupportTicketsPageWrapper />;
}
