import { EmailSettingsPageWrapper } from '@/components/dashboard/components/settings/general';

export const metadata = {
  title: 'Email Settings',
  description: 'Configure email settings and options',
};

export default function EmailSettingsPage() {
  return <EmailSettingsPageWrapper />;
}
