import { CitiesPageWrapper } from '@/components/dashboard/components/location/cities';

export const metadata = {
  title: 'Cities',
};

export default function CitiesPage() {
  return <CitiesPageWrapper />;
}
