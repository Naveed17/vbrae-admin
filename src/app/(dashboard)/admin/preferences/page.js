import { PreferencesPageWrapper } from '@/components/dashboard/components/settings';

export const metadata = {
    title: 'Preferences',
};

export default function PreferencesPage() {
    return <PreferencesPageWrapper />;
}
