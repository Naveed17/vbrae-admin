import { CustomFieldsPageWrapper } from '@/components/dashboard/components/products';

export const metadata = {
  title: 'Custom fields',
  description: ''
};

export default function CustomFieldPage() {
  return <CustomFieldsPageWrapper />;
}
