import { BlockDevicePageWrapper } from '@/components/dashboard/components';
import React from 'react'

export const metadata = { title: 'Block Device' };

function BlockDevicePage() {
    return (
        <BlockDevicePageWrapper />
    )
}

export default BlockDevicePage
