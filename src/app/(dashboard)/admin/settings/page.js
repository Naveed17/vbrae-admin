import { SettingsPageWrapper } from '@/components/dashboard/components/settings/general/settings';

export const metadata = {
    title: 'Settings',
};

export default function SettingsPage() {
    return <SettingsPageWrapper />;
}
